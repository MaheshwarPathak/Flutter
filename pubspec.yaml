name: interest_calculator
description: A Flutter app to calculate simple and compound interest.
publish_to: 'none'
version: 1.0.0+1
environment:
  sdk: '>=2.19.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.0

flutter:
  uses-material-design: true
